// Tracked stub — see wailsjs/go/app/App.ts for the rationale. Web
// builds alias this away via Vite; desktop builds let `wails generate`
// overwrite it with the real runtime bindings.

export * from "../../src/platform/runtime.web";
